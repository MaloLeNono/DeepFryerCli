Only supports Windows x64

# How to use

Simply run the application with the following arguments:
`--input`
`--output`
These are required. The program will not run without them.

The program won't output anything for a few seconds while it deep fries the video. A message saying `File has been deep fried!` will appear once the file has been outputed.
